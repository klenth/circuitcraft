npm install
npm start

Dependencies needed for Menubar:

Dependencies needed for React Flow:
    npm install reactflow
    npm run dev

    import ReactFlow from 'reactflow';
    import 'reactflow/dist/style.css';

Dependencies needed for React Flow Smart Edge:
(for better collision control of nodes and edges)
    npm install @tisoap/react-flow-smart-edge